# Graph RAG Pipeline Requirements - TESTED & WORKING
# Compatible with PyTorch 2.1.0 + CUDA 11.8
# FAISS installed via conda (see environment-*.yml)

# Core ML (installed via conda - see environment.yml)
# pytorch=2.1.0
# pytorch-cuda=11.8 (GPU) or cpuonly (CPU)
# numpy<2.0
# faiss-cpu or faiss-gpu (via conda)

# Step 1: Core transformers (let it pick compatible versions)
transformers>=4.30.0,<4.45.0
tokenizers>=0.13.0

# Step 2: Embeddings (will use existing transformers)
sentence-transformers>=2.2.0,<3.0.0

# Step 3: Specific tools
FlagEmbedding>=1.2.0

# LLM
together>=1.2.0
openai>= 2.8.1
anthropic>= 0.75.0
clean-text[gpl]

# Scopus
pybliometrics>=4.4

# Graph Database
neo4j>=5.0.0

# NLP & ML
nltk>=3.8.0
langdetect>=1.0.9
deep-translator>=1.11.0
scikit-learn>=1.3.0

# Data Processing
pandas>=2.0.0

# Web Scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.65.0
pyyaml>=6.0.0
tiktoken>=0.12.0

# Development
pytest>=7.0.0
jupyter>=1.0.0
ipykernel>=6.0.0